# Profile Frontend (Vite + React + Tailwind + Framer Motion)

## Run
```bash
cd frontend
npm install
npm run dev
```
Open `http://localhost:5173` (backend should run on `http://localhost:8080`).

## Build
```bash
npm run build && npm run preview
```
